<div class="container">
	<% @posts.each do |post| %>
		<h2><%= "#{post.title}" %></h2>
		<p><%= "#{post.author}" %></p>
		<p><%= "#{post.body}" %></p>
		Tags: <% post.tags.map do |t| %>
					<%= "#{t.category}" %>
			  <% end %><br><br>
	<% end %>
	<a href="../">Return Home</a>
</div>